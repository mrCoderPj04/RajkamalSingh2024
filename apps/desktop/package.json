{
  "name": "@sofo-sync/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "SOFO Sync Desktop Client (Electron / Tauri)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  }
}
